//(condition) ? (if true) : (if false)

#include<iostream>
using namespace std;
int main(){
    int x;
    cout<<"enter a no.";
    cin>> x;

    // (condition) ? if true : if false

    (x%2==0) ? cout<<"even no." : cout<<"odd";
}